version: "3"

services:
  flutter-web:
    build:
      context: .
      target: builder
    ports:
      - 3011:3011
    command: [ "flutter", "run", "-d", "web-server", "--web-port", "3011", "--web-hostname", "0.0.0.0" ]